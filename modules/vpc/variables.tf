# Inputs for the VPC module.
# Add variables as the module is implemented (e.g. vpc_cidr, azs,
# public_subnet_cidrs, private_app_subnet_cidrs, private_db_subnet_cidrs,
# enable_nat_gateway, single_nat_gateway, tags).
