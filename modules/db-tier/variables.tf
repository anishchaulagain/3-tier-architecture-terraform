# Inputs: identifier, engine, engine_version, instance_class, allocated_storage,
# db_name, master_username, master_password_secret_arn, subnet_ids,
# security_group_ids, multi_az, backup_retention_days,
# deletion_protection, kms_key_id, tags.
